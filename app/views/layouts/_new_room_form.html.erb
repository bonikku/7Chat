<%= form_with(model: @room, remote: true, class: "d-flex") do |form| %>
  <%= form.text_field :name, class: "form-control", autocomplete: 'off' %>
  <%= form.submit data: { disable_with: false } %>
<% end %>